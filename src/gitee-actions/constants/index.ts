export const GITEE_ACTIONS_OPTIONS = 'gitee-actions-options';

export const enum WorkflowTriggerType {
    PULL_REQUEST = 'pull_request',
    PUSH = 'push',
    ISSUE = 'issue',
    COMMENT = 'comment',
}
